import { DynamicDateInputDirctvDirective } from './dynamic-date-input-dirctv.directive';

describe('DynamicDateInputDirctvDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicDateInputDirctvDirective();
    expect(directive).toBeTruthy();
  });
});
